import 'package:flutter/material.dart';

class BoardPlan extends StatelessWidget {
  const BoardPlan({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
